#JSGF V1.0;

grammar commands;

<action> = avance | va | recule | fonce | demi-tour;

<speed> = lentement | rapidement;

<direction> = vers la (gauche | droite);

<time> = pendant (une | deux | trois) secondes;

public <command> = <action> [ <speed> ] [ <direction> ] [ <time> ];
